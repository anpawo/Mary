function test_all_features() -> void {
    // variables types base
    int x = 5;
    float y = 4.2;
    bool b = true;
    str message = "hello mary!";
    char c = 'm';

    // tableaux structures
    int[2] numbers = [1, 2];
    person { name = "john", age = 30 } personStruct;

    // operateurs
    int result = x + 3 * (2 - 1);
    float division = y / 2.0;

    // conditions
    if (b) {
        result = result + 1;
    } else {
        result = result - 1;
    }

    // boucles
    int sum = 0;
    int i = sum;
    while (i < 10) {
        sum = sum + i;
        i = i + 1;
    }
}